#!/bin/sh


{% for k in range(1, NumberofWANwebclients | int +1) %}
docker exec -d clab-{{networkname}}-Client{{k}}-WANweb bash -c "( crontab -l | grep -v -F 'bash /WANweb.sh' ; echo '0 9 * * 1-5 bash /WANweb.sh' ) | crontab -"
docker exec -d clab-{{networkname}}-Client{{k}}-WANweb bash /Cronjobs.sh
{% endfor %}
