$ORIGIN httpsWANsite.dev.
$TTL 300
@       IN      SOA     ns1.httpsWANsite.dev.      admin.httpsWANsite.dev. (
                        2020111901        ; serial number
                        60                    ; refresh
                        120                     ; retry
                        180                ; expire
                        67                   ; ttl
                        )
        NS      ns1.httpsWANsite.dev.

$ORIGIN httpsWANsite.dev.
$TTL 60
ns1     A       5.5.5.5

@       A       5.20.0.12
